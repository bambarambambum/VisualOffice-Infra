---
  - name: Install a docker
    hosts: all
    become: true
    vars:
      docker_users:
        - uadmin
      pip_install_packages:
        - name: docker
    roles:
      - geerlingguy.docker
      - geerlingguy.pip