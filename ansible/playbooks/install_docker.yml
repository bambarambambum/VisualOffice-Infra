---
  - name: Install a docker
    hosts: do-04
    become: true
    vars:
      docker_users:
        - uadmin
      pip_install_packages:
        - name: docker
    roles:
      - geerlingguy.docker
      - geerlingguy.pip